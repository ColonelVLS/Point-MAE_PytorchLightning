from mae_pipeline import MAESystem
# functions and classes i can keep from model.py
from model import fps, Group, Encoder, Mlp, Attention, Block

import torch
import torch.nn as nn


# Create a version that could be used both as encoder and decoder
# Positional embedding module
# Feature embedding module 

class MaskGenerator(nn.Module):
    def __init__(self, ):
        

class Transformer(nn.Module):

    def __init__(self, ):
        pass








class PointTransformer(MAESystem):

    def configure_networks(self):
        return super().configure_networks()

    def configure_optimizers(self):
        return super().configure_optimizers()
